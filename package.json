{
  "name": "nelia",
  "version": "1.0.0",
  "description": "talk with aliens 👽",
  "scripts": {
    "build": "concurrently \"yarn --cwd ./client/ generate && yarn --cwd ./client/ start\" \"yarn --cwd ./server/ start\"",
    "install": "concurrently \"yarn --cwd ./client/\" \"yarn --cwd ./server/\"",
    "dev": "concurrently \"yarn --cwd ./client/ dev\" \"--exec yarn --cwd ./server/ start\""
  },
  "repository": "https://github.com/emigrek/nelia",
  "author": "Emigrek <kaioshinbox@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^6.2.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}
